import { AfterViewInit, EventEmitter } from '@angular/core';
import { Element as StripeElement, ElementType } from './interfaces/element';
import { ElementsOptions } from './interfaces/elements';
import { StripeService } from './services/stripe.service';
export declare class StripeCardComponent implements AfterViewInit {
    private stripeService;
    private options;
    elementsOptions: ElementsOptions;
    constructor(stripeService: StripeService);
    change: EventEmitter<{
        card: any;
        element: StripeElement;
    }>;
    complete: EventEmitter<{
        card: any;
        element: StripeElement;
    }>;
    error: EventEmitter<any>;
    elementTypes: Array<ElementType>;
    private card;
    private cardElement?;
    private elements?;
    private options$;
    private elementsOptions$;
    ngAfterViewInit(): void;
    getCard(): StripeElement | undefined;
}
